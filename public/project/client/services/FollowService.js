(function() {
    "use strict";

    angular
        .module("BackpackBuddyApp")
        .factory("FollowService", FollowService);

    function FollowService($http, $q) {

        var service = {
        };

        return service;

    }
}());